# DataPulse 脉策智能 / 脉策问数 智能问数系统 · 测试验收指南

| 项目 | 说明 |
|---|---|
| 版本 | 演示版 v1.0 |
| 日期 | 2026-09-11 |
| 数据性质 | **全部为虚构脱敏数据**（占位品牌「星澜集团」），与任何真实企业无关 |
| 验证状态 | ⚠️ 本次交付**未做实机全栈启动验证**，仅完成静态配置校验。若启动失败请按第六节反馈 |

---

## 一、环境准备

| 项目 | 要求 |
|---|---|
| Docker | 20.10+，含 Compose v2（`docker compose version` 可查） |
| 内存 | ≥ 4 GB 可用 |
| 磁盘 | ≥ 5 GB |
| 网络 | 首次构建需拉取镜像与依赖（已预配国内镜像源） |
| 大模型 | **问数功能需自备 API Key**（见 2.1） |

---

## 二、启动步骤

### 2.1 获取代码

```bash
git clone https://github.com/qishen123456/datapivot-demo.git
cd datapivot-demo
```

> 仓库为**公开**，无需 GitHub 账号即可克隆。
> 若本机未装 git，可直接下载 ZIP 解压：
> https://github.com/qishen123456/datapivot-demo/archive/refs/heads/main.zip

### 2.2 准备环境变量

```bash
cp .env.example .env
```

编辑 `.env`，至少确认以下 4 项：

- `DATAPULSE_DB_PASSWORD` —— 数据库密码，自行设定
- `DATAPULSE_AI_API_KEY` —— 大模型 API Key
- `DATAPULSE_AI_BASE_URL` —— 模型服务地址
- `DATAPULSE_AI_MODEL` —— 模型名称

### 2.3 启动服务

```bash
docker compose up -d
```

预期：`postgres` / `migrate` / `backend` / `frontend` 四个服务启动，首次构建约数分钟。

自检：`docker compose ps` —— `datapulse-postgres` 应为 `healthy`。

### 2.4 导入演示数据

```bash
./scripts/load_demo_data.sh
```

预期：输出表清单，末行为 `✔ 脱敏数据已就绪，可以开始问数演示。`

> 该脚本幂等，可重复执行（会先重建 schema 再导入）。

### 2.5 访问系统

浏览器打开 **http://localhost:8888**

---

## 三、登录账号

| 账号 | 密码 | 角色 |
|---|---|---|
| `13800000001` | `12345678` | 管理员 |
| `13800000002` | `12345678` | 普通用户 |

---

## 四、验收清单

### A. 部署与启动

- [ ] **A1** `docker compose up -d` 无报错，4 个服务均启动
- [ ] **A2** `datapulse-postgres` 健康检查为 `healthy`
- [ ] **A3** `load_demo_data.sh` 执行成功并输出表清单
- [ ] **A4** 浏览器可打开 http://localhost:8888，页面正常渲染（F12 Console 无红色报错）

### B. 登录与权限

- [ ] **B1** 管理员账号 `13800000001 / 12345678` 可登录
- [ ] **B2** 登录后进入智能问数主界面
- [ ] **B3** 普通用户 `13800000002 / 12345678` 可登录，且可见功能少于管理员
- [ ] **B4** 错误密码无法登录

### C. 智能问数（核心功能）

> 入口：`/smart-ask`

- [ ] **C1** 可选择数据集，以下 3 个为**启用状态**：
  - 云枢经营总览
  - 磐石成交流水
  - 云雀线上流水
- [ ] **C2** 输入问题后能返回结果（表格 / 图表 / 结论）
- [ ] **C3** 可查看系统生成的 SQL
- [ ] **C4** 多轮追问能保持上下文

**建议测试问题**（系统预置，可直接点击）：

1. 上个月业绩怎么样
2. 各大区上个月指标和实际开单是多少
3. 今年一季度哪个片区最争气
4. 今年哪几个大区增速最快
5. 沧澜战区任务对得上吗
6. 消费者事业群整体达成率是多少
7. 各战区达成率排名
8. 浔澜战区和岱澜战区对比
9. 查看碣澜战区的城市站明细

- [ ] **C5** 抽查勾稽关系：任一**战区**金额 = 其下各**片区**之和（数据按此规则生成）

### D. 数据与组织

- [ ] **D1** 组织树页面 `/organization-trees` 层级为：**事业群 → 战区 → 片区 → 客户经理**
- [ ] **D2** 行业线为：**食饮 / 楼宇办公 / 工业健康**
- [ ] **D3** 数据中不含任何真实企业名、人名、手机号

### E. 管理后台

- [ ] **E1** `/datasets` 数据集管理可打开
- [ ] **E2** `/employee-permissions` 员工权限可打开
- [ ] **E3** `/ai-models` AI 模型配置可打开，参数正确
- [ ] **E4** `/bookshelves` 数据资产书架可打开

---

## 五、已知限制（不作为"验收不通过"项）

1. 本次交付**未做实机全栈启动验证**，配置仅经静态校验。
2. **飞书同步** `/feishu-sync` 未接入真实租户，相关功能不可用。
3. 部分数据集为**停用**状态（如"月度细分行业线"），列表不显示属正常。
4. 若 `.env` 未配置有效 `DATAPULSE_AI_API_KEY`，问数会报模型错误——属**配置问题**而非缺陷。
5. 演示数据为随机扰动生成，**数值不具备业务参考意义**，仅用于验证功能链路。
6. 端口占用：前端 8888、后端 5002、PostgreSQL 5433。若被占用，改 `.env` 中对应 `PORT` 项。

---

## 六、问题反馈格式

```text
【模块】如：智能问数
【问题】一句话描述
【复现步骤】1. …  2. …  3. …
【预期结果】…
【实际结果】…
【截图/日志】F12 Console 截图；服务端日志见下方命令
【环境】操作系统 / 浏览器版本 / Docker 版本
```

**日志采集命令**

```bash
docker compose logs backend  --tail 100   # 后端
docker compose logs frontend --tail 100   # 前端
docker compose logs postgres --tail 100   # 数据库
```

**停止 / 重置**

```bash
docker compose down       # 停止
docker compose down -v    # 停止并清除数据卷（数据需重新导入）
```
