# DataPulse 脉策智能 / 脉策问数 —— 演示环境验收手册

| 项目 | 说明 |
|---|---|
| 版本 | 演示版 v1.1 |
| 手册日期 | 2026-09-14 |
| 对应分支 | `datapivot-demo` |
| 数据性质 | **全部为虚构脱敏数据**（占位品牌「星澜集团」），与任何真实企业无关 |
| 验证状态 | ✅ 本手册的启动流程与全部验收项，均在 macOS + Docker Desktop 上**实机验证**过 |
| 取代关系 | 本手册取代旧版 `测试验收指南.md`（2026-09-11）—— 旧版的容器名、启动顺序、账号角色、页面可达性均已过时 |

---

## 一、环境准备

| 项目 | 要求 |
|---|---|
| Docker Desktop | 需含 Compose v2（`docker compose version` 可查）。**启动前必须先打开 Docker Desktop**，否则所有命令报 `cannot connect to the Docker daemon` |
| 内存 | ≥ 4 GB 可用 |
| 磁盘 | ≥ 5 GB（首次构建需拉取镜像与依赖） |
| 端口 | **8888**（前端）/ **5002**（后端）/ **5433**（PostgreSQL）需空闲 |
| 大模型 | 问数功能需有效 API Key（见 2.2） |

---

## 二、启动步骤（顺序不可颠倒）

### 2.1 获取代码

```bash
git clone -b datapivot-demo https://github.com/qishen123456/datapivot-demo.git
cd datapivot-demo
```

> 仓库为**公开**，无需 GitHub 账号。默认分支是 `main`，**演示版在 `datapivot-demo` 分支**，务必带 `-b`。

### 2.2 准备环境变量

```bash
cp .env.example .env
```

然后编辑 `.env`，**以下关键项必须逐条核对**（这是本项目最容易踩的坑）：

| 类别 | 变量 | 说明 |
|---|---|---|
| 后端读取 | `DATAPULSE_DB_PASSWORD` | 数据库密码，自行设定 |
| 后端读取 | `DATAPULSE_BACKEND_PORT` | **必须为 `5002`**，否则健康检查失败、容器起不来 |
| 后端读取 | `DATAPULSE_DB_HOST` | 容器内**必须为 `postgres`**（服务名，不能写 localhost） |
| 后端读取 | `DATAPULSE_DB_TYPE` | `postgresql` |
| compose 读取 | `SMARTASK_DB_DATABASE` | 与上面同一个库（本项目为 `postgres`） |
| compose 读取 | `SMARTASK_DB_USERNAME` | 与上面同一个用户（本项目为 `postgres`） |
| compose 读取 | `SMARTASK_POSTGRES_PASSWORD` | 与 `DATAPULSE_DB_PASSWORD` **保持一致** |
| 问数功能 | `DATAPULSE_AI_API_KEY` / `DATAPULSE_AI_BASE_URL` / `DATAPULSE_AI_MODEL` | 缺任一项则问数报模型错误 |

> ⚠️ **两套前缀必须同时存在**：后端 Python 代码统一读 `DATAPULSE_*`；而 `docker-compose.yml` 与 `scripts/load_demo_data.sh` 读 `SMARTASK_*`。只配一套会出现"容器起来了但连不上库"或"数据导不进"。
>
> ⚠️ 若 `.env` 缺 `DATAPULSE_DB_PASSWORD`，后端会静默回落到默认 SQLite + 默认密码，表现为查询全部失败。
>
> ⚠️ 数据库密码若以 `enc:v1:` 开头，说明是加密值，需要项目根 `config/.secret_master_key` 才能解密；**缺这个文件会被自动生成随机密钥，导致解密失败**。

### 2.3 先启动数据库

```bash
docker compose up -d postgres
```

自检：`docker compose ps` —— `smartask-postgres` 应显示 `(healthy)`。

### 2.4 再导入演示数据

```bash
./scripts/load_demo_data.sh
```

预期末行输出：`✔ 脱敏数据已就绪，可以开始问数演示。`

> **为什么必须先导数据再起后端**：迁移脚本 `20260627_online_standard_view.sql` 要在数据表 `feishu_tbl_beta` 之上建视图，而该表由本脚本导入。全新环境先起后端的话，该迁移会**自动跳过**（不报错，但视图不会被创建）。
>
> 该脚本会**重建 public schema**（`DROP SCHEMA ... CASCADE`），即清空既有数据后重新导入，可安全重复执行。**但它会清掉该库里的其他数据** —— 若把本项目与别的项目共用同一个 PostgreSQL 实例/数据卷，请先确认。

### 2.5 启动其余服务

```bash
docker compose up -d
```

预期：`smartask-postgres` / `smartask-migrate` / `smartask-backend` / `smartask-frontend` 全部启动，前端容器在 backend 健康后才启动（首次构建约数分钟）。

> `smartask-migrate` 是**一次性任务**，执行完即退出（`Exited (0)`），这是正常现象，不是故障。

### 2.6 访问系统

浏览器打开 **http://localhost:8888**

---

## 三、演示账号

| 账号 | 密码 | 角色 | 实际可访问范围（实测） |
|---|---|---|---|
| `13800000001` | `12345678` | 业务管理员 | 智能分析工作台、SQL调试台、数据书架 |
| `13800000002` | `12345678` | 普通用户 | 智能分析工作台、SQL调试台 |

> 账号均为虚构号码。登录页已预填 `13800000001`，输入密码即可。
>
> ⚠️ **本演示环境当前没有可用的超级管理员账号**，且上述两个账号均为低权限角色 —— 管理类页面会**被路由守卫拦截并自动跳回工作台**。详见第五节 5.1 / 5.2，验收前请先阅读。

---

## 四、验收清单

### A. 部署与启动

- [ ] **A1** `docker compose up -d postgres` 无报错，`smartask-postgres` 为 `healthy`
- [ ] **A2** `./scripts/load_demo_data.sh` 执行成功，末行输出"✔ 脱敏数据已就绪"
- [ ] **A3** `docker compose up -d` 后 `docker compose ps` 中 postgres/backend/frontend 均为 `healthy`
- [ ] **A4** 浏览器打开 http://localhost:8888 正常渲染，F12 Console 无红色报错

### B. 登录与权限

- [ ] **B1** `13800000001 / 12345678` 可登录，右上角显示"演示管理员 / 管理员"
- [ ] **B2** 登录后进入智能分析工作台
- [ ] **B3** 故意输错密码无法登录，页面给出错误提示
- [ ] **B4** 未登录状态下直接访问 `http://localhost:8888/datasets`，会被拦回登录页/工作台

### C. 智能问数（核心功能，重点验收）

> 入口：`/smart-ask`。系统共 3 个**启用**数据集：**云枢经营总览**、**磐石成交流水**、**云雀线上流水**（另有 2 个停用数据集不显示，属正常）。

- [ ] **C1** 可切换上述 3 个数据集
- [ ] **C2** 输入问题后能返回结果（表格 / 图表 / 结论），且给出可展开的 SQL
- [ ] **C3** 多轮追问能保持上下文

**建议测试问题**（均为系统预置，界面上可直接点击）：

| 数据集 | 推荐问题 |
|---|---|
| 云枢经营总览 | 榕岭战区的业绩 |
| 云枢经营总览 | 云溟战区和沐阳战区对比 |
| 云枢经营总览 | 消费者事业群各战区业绩排名 |
| 磐石成交流水 | 澔原战区业绩怎么样 |
| 磐石成交流水 | 四大战区区域业绩整体盘点 |
| 磐石成交流水 | 客户经理年度开单金额 Top10 是谁 |
| 云雀线上流水 | 电商事业群行业部业绩排名 |
| 云雀线上流水 | 云集直营和澜庭直营对比 |
| 云雀线上流水 | 电商事业群前5的细分业务 |

- [ ] **C4** 抽查勾稽关系：任一**战区**金额 = 其下各**片区**之和（数据按此规则生成）
- [ ] **C5** 组织维度层级为：**事业群 → 战区 → 片区 → 客户经理**；行业线为**食饮 / 楼宇办公 / 工业健康**

### D. SQL 调试台

- [ ] **D1** `/sql-debug` 可打开，页面标题为"SQL调试台"
- [ ] **D2** 可选择一个数据集并执行一段简单 SQL，返回结果集

### E. 管理后台（⚠️ 当前版本不可验收，原因见 5.1）

以下页面在本演示环境**当前账号体系下无法访问**（会被路由守卫跳回工作台）：

- `/agents` 智能体编排配置
- `/datasets` 数据资产管理
- `/databases` 数据连接管理
- `/ai-models` 模型服务配置
- `/report-config` 报告模板配置
- `/advanced-capabilities` 进阶能力中心
- `/feishu-sync` 飞书数据同步
- `/runtime-migration` 迁移发布管理（超管专属）
- `/organization-trees` 组织树管理（超管专属）
- `/employee-permissions` 角色权限管理（超管专属）
- `/admin-console` 系统控制台（超管专属）

> 这不是页面本身有缺陷 —— 前端界面完整、布局统一（实测 10 个页面均无溢出/错位）。**是账号角色配置的问题**，修复方式见 5.1 / 5.2。补齐后可全量验收。

---

## 五、已知问题（请验收方知悉）

### 5.1 无可用超级管理员账号 —— 4 个页面永久不可达 ⚠️

两个缺陷叠加导致：

1. `.env` 中只有 `SMARTASK_ADMIN_*`，**缺少 `DATAPULSE_ADMIN_*`**；而后端 `backend/controllers/auth.py` 只读 `DATAPULSE_ADMIN_USERNAME/PASSWORD` → 超管账号从未被创建。
2. `backend/controllers/auth.py` 的角色清洗函数白名单为 `{admin, business_admin, user}`，**漏了 `super_admin`** → 即使手工在 `config/employee_permissions.json` 里把某人设为 `super_admin`，也会被**静默降级为普通用户**（无任何日志报错）。

**后果**：`/runtime-migration`、`/organization-trees`、`/employee-permissions`、`/admin-console` 无法打开。

**修复**（各一行）：`.env` 补 `DATAPULSE_ADMIN_USERNAME / DATAPULSE_ADMIN_PASSWORD / DATAPULSE_ADMIN_DISPLAY_NAME`；`_clean_role()` 白名单补 `super_admin`。

### 5.2 演示账号权限偏低 —— 7 个管理页不可达

`13800000001` 的真实角色是 `business_admin`（等级 2），而管理类页面要求 `admin`（等级 3）。
登录页/README 里它被描述为"管理员"，容易让人误以为能看全部 —— **实际只能进 3 个页面**。

**修复**：把该账号角色改为 `admin`，或在 5.1 修复后用超管账号验收。

### 5.3 文案与数据中有待清理的字符串残留

约 **900 处**非预期字符串残留，分布在飞书同步页、系统事件日志表、演示数据 dump 中（均为脱敏脚本批量替换时误伤，本应是"成功"这类中性词）。

**影响**：不影响任何功能链路，问数与页面渲染均正常；但若验收标准包含"文案零异常"，此项需先清理再验。

### 5.4 README 命名与实际不符

`README.md` 中写的是 `datapulse-*` 容器名与 `DATAPULSE_*` 变量，**实际** compose 与脚本用的是 `smartask-*` / `SMARTASK_*`。请以本手册第二节为准。

### 5.5 其他

1. **飞书同步** `/feishu-sync` 未接入真实租户，相关功能不可用。
2. 数据集 `沙盘验证样例`(id 13)、`苍梧品类脉络`(id 63) 为**停用**状态，列表不显示属正常。
3. 云枢经营总览数据集中，含"上个月"等**相对时间词**的问题可能被误判为组织节点名而返回 0 行，建议改用预置问题。
4. 若 `.env` 未配置有效 AI Key，问数会报模型错误 —— 属**配置问题**而非代码缺陷。
5. 演示数据为随机扰动生成，**数值不具备业务参考意义**，仅用于验证功能链路。
6. 端口被占用时改 `.env` 中对应 `PORT` 项。
7. **切换本项目与其他 Docker 项目时**：两者容器名均为硬编码 `smartask-*`，不能共存，需先 `docker rm -f` 对方容器。

---

## 六、问题反馈格式

```text
【模块】如：智能问数
【问题】一句话描述
【复现步骤】1. …  2. …  3. …
【预期结果】…
【实际结果】…
【截图/日志】F12 Console 截图；服务端日志见下方命令
【环境】操作系统 / 浏览器版本 / Docker 版本
```

**日志采集命令**

```bash
docker compose logs backend  --tail 100
docker compose logs frontend --tail 100
docker compose logs postgres --tail 100
docker compose logs migrate  --tail 100   # 迁移失败时看这个
```

**停止 / 重置**

```bash
docker compose down       # 停止（保留数据）
docker compose down -v    # 停止并删除数据卷（数据需重新执行 2.4 导入）
```

**状态自检**

```bash
docker compose ps                                                  # 容器状态
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8888/    # 前端，期望 200
docker exec smartask-postgres psql -U postgres -d postgres -c "\dt"  # 数据表清单
```
